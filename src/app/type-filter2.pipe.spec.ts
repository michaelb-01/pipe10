import { TypeFilter2Pipe } from './type-filter2.pipe';

describe('TypeFilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new TypeFilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
